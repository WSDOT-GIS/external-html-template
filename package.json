{
  "name": "external-html-template",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && bun build ./src --outdir ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.5.2"
  }
}